# Только зависимости сервера (bin/). Для сборки Docker — без Flutter.
name: dart_personal_website_server
description: Server-only dependencies for Docker build.
version: 1.0.0

environment:
  sdk: ^3.10.1

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  shelf_static: ^1.1.3
